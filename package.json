{
  "name": "movie-verse-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "npm run dev -w backend",
    "build:frontend": "npm run build -w frontend",
    "build:backend": "npm run build -w backend",
    "build": "npm run build -w frontend && npm run build -w backend && (if [ -d frontend/.vercel/output ]; then rm -rf .vercel/output && mv frontend/.vercel/output .vercel/output; fi)"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
